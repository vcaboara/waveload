[options]
packages = find:
package_dir =
    = waveload
install_requires =

[options.packages.find]
where = waveload

[options.extras_require]
dev =
    pytest
    coverage
    pytest-cov
    pre-commit

